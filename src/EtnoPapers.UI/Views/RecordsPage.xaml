<Page x:Class="EtnoPapers.UI.Views.RecordsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="White"
      Foreground="Black">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="40,40,40,0">
            <TextBlock Text="Registros" FontSize="28" FontWeight="Bold" Foreground="#0078D4" Margin="0,0,0,8"/>
            <TextBlock Text="Visualize, filtre e gerencie seus registros de artigos extraídos" FontSize="14" Foreground="#505050" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Filter Panel -->
        <Border Grid.Row="1"
                Background="#F3F3F3"
                BorderBrush="#E0E0E0"
                BorderThickness="0,1,0,1"
                Padding="40,20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Buscar:" FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox Width="250" Padding="8,6" Background="White" BorderBrush="#E0E0E0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </Border>

        <!-- Records List -->
        <StackPanel Grid.Row="2" Margin="40,20">
            <TextBlock Text="Nenhum registro encontrado" FontSize="14" Foreground="#505050" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,60,0,0"/>
            <TextBlock Text="Use a seção Upload PDF para adicionar novos registros" FontSize="12" Foreground="#8A8A8A" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,8,0,0"/>
        </StackPanel>
    </Grid>

</Page>
