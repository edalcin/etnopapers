<Window x:Class="EtnoPapers.UI.Views.DeleteConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Confirmar Deletar"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        Background="{StaticResource Background}"
        Foreground="{StaticResource Text}"
        Width="400"
        Height="250"
        ResizeMode="NoResize">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Warning Icon and Title -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="⚠️" FontSize="32" Margin="0,0,12,0" VerticalAlignment="Top"/>
            <StackPanel VerticalAlignment="Top">
                <TextBlock Text="Confirmar Exclusão"
                           Style="{StaticResource Heading2}"
                           Margin="0,0,0,4"/>
                <TextBlock Text="Esta ação não pode ser desfeita"
                           Style="{StaticResource Caption}"
                           Foreground="{StaticResource Error}"/>
            </StackPanel>
        </StackPanel>

        <!-- Message -->
        <TextBlock Grid.Row="1"
                   x:Name="MessageTextBlock"
                   Style="{StaticResource Body}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,24"/>

        <!-- Details Box -->
        <Border Grid.Row="2"
                Background="{StaticResource Surface}"
                BorderBrush="{StaticResource Border}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12"
                Margin="0,0,0,24">
            <TextBlock Text="Você está prestes a deletar 1 ou mais registros permanentemente. Os dados não poderão ser recuperados."
                       Style="{StaticResource Caption}"
                       TextWrapping="Wrap"
                       Foreground="{StaticResource TextSecondary}"/>
        </Border>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cancelar"
                    Style="{StaticResource DefaultButtonStyle}"
                    Padding="16,8"
                    Margin="0,0,8,0"
                    IsCancel="True"
                    Background="Transparent"
                    Foreground="{StaticResource Primary}"/>

            <Button Content="Deletar"
                    Style="{StaticResource DefaultButtonStyle}"
                    Padding="16,8"
                    Background="{StaticResource Error}"
                    Foreground="White"
                    IsDefault="True"
                    Click="DeleteButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
